<p id="notice">
  <%= notice %>
</p>

<b><font size="6">
  提出物詳細
</font></b>

<p>
  <strong>科目</strong>
  <%= @submission.subject %>
</p>

<p>
  <strong>タイトル</strong>
  <%= @submission.title %>
</p>

<p>
  <strong>期限</strong>
  <%= @submission.deadline %>
</p>

<p>
    <%= @submission.grade %>
  <strong>年</strong>
    <%= @submission.class_room %>
  <strong>組</strong>
</p>

<table align="left">
<thead>

 <tr>
   <th>名前</th>
 </tr>
</thead>

<tbody>
 <% @members.each_with_index do |member,index| %>
   <tr>
     <td align="center"><%= member.name %></td>
   </tr>
 <% end %>
</tbody>
</table>

<table>
  <tr><th>提出状況</th></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
  <tr><td><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td></tr>
</table>

<table>
  <tr>
   <td>
     <% if @submission.filename.present? %>
       <%= image_tag @submission.filename %>
     <% end %>
    </td>
  </tr>
  
</table>

<%= link_to 'Back', submissions_path %>
