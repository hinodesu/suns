<b><font class="s-title">
  提出物詳細
</font></b>

<p id="notice">
  <%= notice %>
</p>

<div class="s-sub"></div>
  <div class="s-sub s-sub2"></div>
  <div class="s-sub s-sub3"></div>

<p class="fs25">
  <strong>科目</strong>
  <%= @submission.subject %>
</p>

<p class="fs25">
  <strong>タイトル</strong>
  <%= @submission.title %>
</p>

<p class="fs25">
  <strong>期限</strong>
  <%= @submission.deadline %>
</p>

<p class="fs25">
    <%= @submission.grade %>
  <strong>年</strong>
    <%= @submission.class_room %>
  <strong>組</strong>
</p>

<table border = "1" width = "100%">
  <thead class = "s-hyou2">
    <tr>
      <th class="fs25">名前</th>
      <th class="fs25">提出状況</th>
    </tr>
  </thead>
  <tbody class = "s-hyou">
    <% @members.each_with_index do |member,index| %>
      <tr>
       <% if @submission[Submission.column_names[ index + 7 ].to_sym] == 0 %>
         <td align="center" style="color:#ff0000" class="fs20"><%= member.name %></td>
       <% elsif @submission[Submission.column_names[ index + 7 ].to_sym] == 1 %>
         <td align="center" style="color:#ffa500" class="fs20"><%= member.name %></td>
       <% else %>
         <td align="center" class="fs20"><%= member.name %></td>
       <% end %>
       <td class="fs20"><%= SUBMISSION_LIST[@submission[Submission.column_names[ index + 7 ].to_sym]] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to '編集', edit_submission_path(@submission),class: "submit" %> |
<%= link_to '戻る', submissions_path,class:"submit" %>
